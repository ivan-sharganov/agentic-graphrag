You are a triple-annotation generator (gold facts) for GraphRAG.

GOAL
Build a dataset of (question, triples) pairs, where triples are:
1) Triples that directly answer the question (evidence triples),
2) Triples that connect evidence to the root Encounter node (connection triples),
3) Triples that establish scope (passport control in Spain) — only if such relations are explicitly present in Cypher (scope triples).

QUESTION
Which question topics are most common at passport control in Spain?

SOURCE OF TRUTH
The only source of truth is the Cypher file text. No guesses, world knowledge, or extensions. If something is not in Cypher, do not add it.

INPUT DATA
Use ALL Cypher files cypher_i.txt from folders:
- /Users/i.sharganov/🟩🟩🟩/обучение/МАГА/мага_обучение/3_сем/Диплом/эксперимент/neo4j_граф/cypher_checkpoints/germany
- /Users/i.sharganov/🟩🟩🟩/обучение/МАГА/мага_обучение/3_сем/Диплом/эксперимент/neo4j_граф/cypher_checkpoints/spain
- /Users/i.sharganov/🟩🟩🟩/обучение/МАГА/мага_обучение/3_сем/Диплом/эксперимент/neo4j_граф/cypher_checkpoints/italy
- /Users/i.sharganov/🟩🟩🟩/обучение/МАГА/мага_обучение/3_сем/Диплом/эксперимент/neo4j_граф/cypher_checkpoints/fr_1
- /Users/i.sharganov/🟩🟩🟩/обучение/МАГА/мага_обучение/3_сем/Диплом/эксперимент/neo4j_граф/cypher_checkpoints/fr_2

OUTPUT (annotation files)
For each cypher_i.txt, create JSON:
- .../neo4j_граф/annotation/q_63/germany/annotation_i.json
- .../neo4j_граф/annotation/q_63/spain/annotation_i.json
- .../neo4j_граф/annotation/q_63/italy/annotation_i.json
- .../neo4j_граф/annotation/q_63/fr_1/annotation_i.json
- .../neo4j_граф/annotation/q_63/fr_2/annotation_i.json

If a specific cypher_i.txt has no suitable triples, triples must be an empty array.

SEMANTIC CONSTRAINT
Find question topics: Questioning.topicKey. Include triples Encounter->hasQuestioning->Questioning and Questioning->topicKey->'...'.

DO NOT INCLUDE
- Any “advice”, “usually”, “always” — unless implemented in the graph via corresponding nodes/relationships.
- Any facts not connected to Encounter.

CONTROL STAGE VALIDITY (important)
If an Encounter has controlStage and it does NOT refer to passport control at the desk (e.g., AtGate / AtCheckin and similar), then do NOT include DocumentCheck/Questioning/BiometricCheck from that Encounter.
If controlStage is absent, assume passport control only if the necessary relationships are explicitly present in Cypher (DocumentCheck/Questioning/BiometricCheck).

PROCEDURE FOR EACH cypher_i.txt
Step 1. Find all Encounter nodes present in the file (usually MERGE (n:Encounter {key:...})).
Step 2. For each Encounter, verify that it is passport control (see the controlStage rule).
Step 3. Keep only Encounter nodes that can be linked to Spain via atCountry or via atCity/atAirport + locatedInCountry.
Step 4. Build triples minimally:
  A) evidence: the links/nodes that directly answer the question
  B) connection: Encounter -> ... (connects evidence to Encounter)
  C) scope: Encounter -> atCity/atCountry/atAirport and related locatedInCity/locatedInCountry, if present.

Semantic matching is allowed (e.g., airport ↔ city from the list), even if there is no explicit locatedInCity/locatedInCountry link.

OUTPUT FORMAT (strict)
JSON object with two fields: question and triples.

- question: string exactly as in the task.
- triples: array of objects, each object is one triple.

Each triple:
{
  "s": {"label": "...", "key": "..."},
  "p": "...",
  "o": {"label": "...", "key": "..."}
}

Formatting requirement:
- In each triple, fields "s", "p", "o" must be on separate lines (as in the example).
- Values inside "label"/"key"/"p" must be on one line.

TEMPLATE (do not change the structure)
{
"question": "Which question topics are most common at passport control in Spain?",
  "triples": []
}

NOTE
Use EXACTLY the label/predicate/key that actually appear in cypher_i.txt.
